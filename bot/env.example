# bot/env.example — PLACEHOLDER ONLY. Never put a real token here, never commit a real env.
# Real values live at ~/bots/pulse_bot/env on the VPS (mode 600, hand-placed by Ivan)
# and at repo-root .env locally. Both are gitignored; neither is ever committed.
BOT_TOKEN=
CHAT_ID=
