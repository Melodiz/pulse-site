pulse-site is a static reading layer for AI digests: hand-written HTML, no build step, no dependencies.
docs/ is the published site (GitHub Pages, main branch, /docs).
Digest pages are rendered per-digest by Claude Code from markdown drops in inbox/.
